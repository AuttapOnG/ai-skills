# OS / editor noise
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# AI tools' local-only state (never publish)
.claude/settings.local.json
.codex/
.gemini/
.aider*
.cursor/
.copilot/

# Local-only scan config (company-internal patterns — never publish)
.scan-local-patterns

# Scratch files agents may create during long tasks
WORKING_STATE.md
