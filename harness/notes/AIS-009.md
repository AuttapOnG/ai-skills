# AIS-009 — Generalize worklog and publish

**Status:** done (2026-07-22)

## What shipped
`skills/worklog/SKILL.md` — the personal worklog command, generalized for public release.
Executed subagent-driven (Codex CLI, ~47k tokens on its quota); host pre-staged the raw source
UNCOMMITTED, Codex parameterized it, host ran a strict hygiene gate + reviewed before any commit.

Split personal/company config → a `## Configure` section with placeholders (originals not
reproduced here — writing them would re-leak them):
- `REPOS_ROOT` (was a personal absolute repos path), `GIT_AUTHOR` (was a hardcoded personal
  author-name filter), `TZ_OFFSET_HOURS` (was a hardcoded local offset), `PROJECT_PATH_PREFIX`,
  `<YOUR_WORKLOG_DATASOURCE_ID>` (was a real private Notion database id), a generic ticket
  pattern `PROJ-XXXX` (was a company ticket prefix), and `<YOUR_PROJECT_TAG>`.
- Notion kept as the *example* notes system; Claude/Codex as *example* assistants whose session
  logs you scan. Both Python snippets preserved and compile-checked.

## Verification (strict — hygiene is critical here)
- Gate scanned tracked files for personal/company tokens (absolute home paths, org name, ticket
  prefix, the private Notion id, and the terms in the git-ignored `.scan-local-patterns`) →
  zero hits before commit. Added the org name to `.scan-local-patterns` so the scan catches it.
- Frontmatter + footer present; markdown-only; both python blocks compile; registry regenerated
  (8 skills), in sync; committed + pushed clean (`55506ea`).
