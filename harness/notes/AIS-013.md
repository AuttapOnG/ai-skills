# AIS-013 — skill-updater: discover skills new in the registry

**Status:** done (2026-07-22) — landed via PR #2 (`9ff443e`), backfilled to harness after merge.

## What shipped
Enhancement to `skills/skill-updater/SKILL.md` (builds on AIS-012). Closes the blind spot
where updating only *installed* skills would miss skills **added to the registry** since the
user last looked.
- New **Step 3 — discover skills new in the registry**: for each distinct `source_repo`,
  fetch the machine-readable index (e.g. `registry.json` at repo root, named by the README);
  any entry whose `name` has no matching local folder is "new in the registry". Collect
  `name` + `description` only — don't install yet (new skills are opt-in; an absent skill is
  not a defect). If the index can't be fetched, say so and continue with updates only.
- Preview table (Step 4) gained a **new-in-registry** row type; the confirm prompt now asks
  separately which new skills to install.
- Step 6 covers fresh installs (nothing to replace — same fetch, fresh folder).
- Verify checklist (Step 7) gained an item: new-in-registry skills reported and installed
  only on explicit confirmation, never auto-installed.

Diff was +34/−11 on the SKILL.md, +registry.json bump. Co-author on `9ff443e`: Claude Fable 5.

## Process note
Same backfill situation as AIS-012 — merged via PR with no `harness/` touch. Recorded here
as a distinct feature per the user's chosen 2-feature split (base flow vs. discovery
enhancement).
