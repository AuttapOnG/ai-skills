# AIS-006 — CONTRIBUTING.md + branch protection

**Status:** in_progress (2026-07-22)

## Done
- `CONTRIBUTING.md` (full): ground rules (markdown-only, tool-neutral, frontmatter+footer,
  generated registry, Co-Authored-By), how-to-contribute (owner push / fork+PR), review
  checklist, and **security red flags — reject on sight** (data-exfiltration, out-of-scope
  file access, encoded blobs, prompt-injection, disguised network side effects) per spec §6.2.

## Remaining — branch protection (deferred to end of project)
Deliberately deferred until after all direct-push work (Phase 3) is done, so requiring PRs
doesn't block the owner's remaining pushes. Plan: protect `main` requiring the `validate`
status check + 1 PR review for non-owners, with admin (owner) bypass allowed — matching
spec §6.2 ("non-owner changes only via PR + owner approval"). Needs user confirmation (admin action).
