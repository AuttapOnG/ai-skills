# AIS-014 — init.sh skill↔harness parity guard (drift prevention, Layer 2)

**Status:** done (2026-07-22)

## What shipped
New section "3b. Skill/harness parity" in [init.sh](../../init.sh), between the registry-sync
check and git status. For every `skills/<name>`, it word-boundary-greps the folder name across
**all** of `harness/` (`grep -rqw "$n" harness/`); any skill referenced nowhere → `WARNING`
listing it, pointing at `harness/README.md`. Clean state prints `OK: every skill is tracked`.
Guarded by `[ -d skills ] && [ -f harness/feature_list.json ]` so it no-ops in a fresh repo.

## Why grep all of harness/, not just feature_list.json
AIS-002 batch-migrated 6 skills (cli-subagents, commit, enhance, init-harness, pr, self-review)
under one feature title that names none of them individually — they're named only in
`notes/AIS-002.md`. Scoping the check to feature_list.json would false-warn on all 6 every
session → noise → ignored guard. Empirical test confirmed every current skill name resolves to
≥1 file across `harness/`, so the guard is quiet on correct state.

## Verification
- Quiet on current state: `OK: every skill is tracked in harness/` ✓
- Negative test: `ghost-skill-xyz` → UNTRACKED (would WARN); `skill-publisher`/`skill-updater`
  → tracked ✓
- This guard is exactly what would have surfaced skill-updater at session start (0 harness refs
  before the AIS-012/013 backfill).

## Scope note
Deliberately **not** added to public CI (`validate.yml`): the harness is maintainer-private, and
a CI parity check would fail external PRs that (correctly) don't touch `harness/`. init.sh is the
maintainer's per-session gate — the right home for an internal check with zero contributor friction.
