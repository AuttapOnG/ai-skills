# AIS-002 — Migrate & convert skills per spec §7

**Status:** done (2026-07-22)

Acceptance criteria: spec §8 + harness/feature_list.json (AIS-002).

## What shipped (6 skills, markdown-only, tool-neutral, footer + frontmatter)

| Skill | Source | Key transforms |
|---|---|---|
| self-review | ~/.claude/commands/self-review.md | +name; Read/Edit/AskUserQuestion → plain verbs |
| commit | ~/.claude/commands/commit.md | +name; lint via discovery ladder; **co-author contradiction fixed** ("Do not add Co-Authored-By" → credit all co-authors) |
| enhance | ~/.claude/commands/enhance.md | +name; `$ARGUMENTS` → plain text; Grep/Glob → "search the codebase" |
| pr | ~/.claude/commands/pr.md | +name; **removed `<issue-tracker-host>`**; issue-tracker/team chat/issue tracker → generic tracker + team chat |
| cli-subagents | ~/.claude/skills/cli-subagents | watchdog.sh inlined as fenced block (file dropped); "Claude tokens"→"coordinating agent"; "PreToolUse hooks"→"runtime pre-execution hooks"; `~/.codex` path removed |
| init-harness | ~/.claude/skills/init-harness | surgical (14/588 lines): runtime-discovery framing + "(or your runtime's equivalent)" annotations; 4 support files unchanged |

## Execution method
Subagent-driven with **Codex CLI** as executor (user request). Host pre-staged raw sources
into `skills/` (uncommitted), Codex (gpt-5.6-sol, sandbox workspace-write) did the in-place
neutralization on its own quota (~84k tokens), then host reviewed every diff, ran leak/gate
scans, and made the commits. Plan + Codex task file: `harness/plans/` and session scratchpad.

## Decisions / deviations
- **xlsx-safe-export DEFERRED** → new feature **AIS-011**. Spec §7 lists it for Phase 1, but its
  core is `validate_xlsx_safe.py` (a validator) — §6.2/§9 forbid shipping scripts. Not silently
  dropped; tracked as its own feature pending a policy decision. (worklog=AIS-009,
  update-tags=local-forever, both correctly out of scope here.)
- Host caught 2 quality nits post-Codex and fixed them: `pr` had an instruction sentence inside
  the paste-ready review template (→ `Ticket: <ticket-url>` placeholder); `enhance`'s
  `$ARGUMENTS` replacement read circularly (→ "the text provided with this skill").

## Verification (all clean)
- Leak scan (`/Users/`, `~/.claude`, `~/.codex`, `<company>`, `<company>`, `<company>`, `tracker-host`): none.
- Markdown-only: no non-`.md` files under skills/.
- Every SKILL.md: `name` + `description` frontmatter + standard footer (verbatim).
- 6 commits, each with `Co-Authored-By` trailers for Claude + Codex.

## Gotchas for future migrations
- Codex leaves plan checkboxes unticked and notes empty — trust the file diffs, not its summary.
- Never commit a raw source with a leak (e.g. pr.md's `<company>`); pre-stage uncommitted,
  neutralize, verify, THEN commit — the leak must never enter this public repo's git history.
