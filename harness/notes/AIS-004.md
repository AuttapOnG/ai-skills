# AIS-004 — Registry generator + first registry.json + UC1 acceptance

**Status:** done (2026-07-22)

Acceptance criteria: spec §8 (Phase 1) + harness/feature_list.json (AIS-004).

## What shipped
- `tools/gen_registry.py` — maintainer-only generator (Python 3 stdlib). Scans
  `skills/*/SKILL.md` frontmatter → `registry.json` (schema §4.1: `generated_at` + sorted
  `skills[]` of `{name, description, path, updated}`; `updated` = last git commit date per path).
  CLI: `--write` (regenerate), `--check` (diff vs committed, ignoring `generated_at`, exit 1 on
  drift). Written by Codex CLI (subagent), reviewed + integrated on host.
- `registry.json` — generated, 6 skills.
- `init.sh` — registry-sync step now runs `gen_registry.py --check` (authoritative), with a
  count-based fallback when python3/generator absent. Drift test confirmed: WARNING on mismatch.

## UC1 acceptance — real install on this machine (spec §8)
Pushed `main` → origin (public) at commit `5fd9670`, then performed a genuine UC1 install
fetching from GitHub (raw + API), into a scratch "tool skills dir" (avoids clobbering the live
`~/.claude/skills`; publish-line shown, not written — per plan Task 13):
1. Fetched `registry.json` from raw GitHub → 6 skills.
2. Picked `self-review`; got latest commit SHA for `skills/self-review` via GitHub API →
   `5b45f642…` (resolves to the self-review commit).
3. Raw-fetched only `skills/self-review/SKILL.md` into the tool dir.
4. Wrote `.provenance.json` (schema §4.2) with the real `commit_sha`.

**Verify checklist — all passed:**
- ✅ skill in tool's own dir; `SKILL.md` present with frontmatter + standard footer.
- ✅ `.provenance.json` valid JSON; `commit_sha` resolves to a real commit.
- ✅ publish-line matches README/spec §4.4 verbatim.
- ✅ idempotent — re-run detects the existing `.provenance.json`.

## Notes / decisions
- The `git push` (first public release of Phase 1) was pre-approved by the user
  ("genericize all, then push") after a pre-push hygiene pass that genericized company strings
  in maintainer docs and moved scan patterns to a git-ignored `.scan-local-patterns`.
- `updated` dates all show 2026-07-22 (the AIS-002 commit date per path).
