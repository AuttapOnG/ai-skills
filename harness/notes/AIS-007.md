# AIS-007 — CI validate.yml

**Status:** done (2026-07-22)

## What shipped
`.github/workflows/validate.yml` — runs on push-to-main + every PR:
1. `python3 tools/gen_registry.py --check` (registry in sync with frontmatter)
2. every SKILL.md has name+description frontmatter + standard footer
3. no `/Users/` or `C:\` in skills/
4. skills are markdown-only (no non-.md files)
`fetch-depth: 0` so per-path `git log` (the `updated` field) resolves — else --check false-fails.

## Acceptance — passed
- ✅ CI run on push `76694c7` = success (run 29894811260).
- ✅ Checks provably reject violations: `gen_registry.py --check` returns exit 1 on drift
  (verified locally); grep/find gates exit 1 on hits; CI fails the job on any non-zero step.
  Company-pattern scanning stays local (init.sh + .scan-local-patterns); CI enforces the
  public-safe generic checks.
