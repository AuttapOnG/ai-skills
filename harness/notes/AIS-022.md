# AIS-022 — handoff skill

**Status:** done (2026-07-23)

## What shipped
[`skills/handoff/SKILL.md`](../../skills/handoff/SKILL.md) — compact the current conversation
into a self-contained handoff document a fresh agent can resume from.

## Origin & IP hygiene
Concept from an external 16-line tool-specific command (tool-bound frontmatter, no document
structure). Rewritten from scratch as a full playbook; no text reused, no org references.

## Key decisions
- Expanded the concept into a document skeleton: state / decisions-with-reasons /
  next steps / artifact pointers / environment notes / suggested skills / open questions.
  "Pointers, not copies" and the redaction pass were kept from the original concept and made
  explicit checklist items.
- Location discovery ladder (runtime scratch dir → OS temp → repo convention only on
  explicit request) instead of the original's fixed "OS temp" instruction.
- Added the "fresh-agent test" as the verify criterion: resumable from the document +
  referenced artifacts alone.

## Verification
- `gen_registry.py --write` → 12 skills, in sync; `bash init.sh` all green.
- Company-marker grep: clean. Single-line frontmatter; standard footer.
