# AIS-027 — init-harness: dogfood fixes + committed evals

**Status:** done (2026-08-19)

## What shipped
Source: first real-world run of the skill (2026-08-19, a Claude Design canvas
project — non-code, non-git). Three gaps the executing agent had to improvise
around, now fixed in the skill, plus the eval files that capture the run:

- **Free-text Q5** (`SKILL.md`): the checkpoint question now welcomes free-text
  answers, and the mapping table gains a row — any non-canned answer → compose a
  custom PreToolUse hook from the stated intent using observable command
  patterns, and test the regex against blocked AND allowed samples before
  writing settings.json. (Observed: user answered "only irreversible git
  actions — plain file deletion is fine", which matched no canned option.)
- **Verification analog for no-test projects** (`work-control.md` generation
  note): if the survey found no test suite, append one line to Verification
  Tiers stating what "tests pass" means in this project (e.g. design canvas →
  open canvas, verify artboard vs spec, capture screenshot). Prevents
  generating a harness that tells agents to run tests that don't exist.
- **git init in Step 5** (`SKILL.md`): if the project is not a git repo,
  `git init -b main` first and record it in the cross-cutting log.
- **Evals committed**: `harness/evals/init-harness-greenfield.md` (non-code,
  non-git init: 18 artifacts, custom-Q5 hook behavior, spec-grep, no invalid
  allow glob, verification analog present) and
  `harness/evals/init-harness-converge.md` (existing CLAUDE.md + feature_list
  preserved, sections merged not duplicated). Baselines document both the
  no-skill behavior and the pre-AIS-027 skill behavior observed in the dogfood.
- Verified: 2× Haiku smoke tests — free-text Q5 rule generalizes to a database
  checkpoint (correct row, observable patterns, regex test plan); analog rule
  correctly skips a pytest project and fires for a docs-only project.

## Deliberately not done
- init.sh drift checks (in_progress-without-note, count mismatch): the staleness
  actually observed (progress.md claiming "pending push" after push) is semantic,
  not structural — those checks would not have caught it. Revisit when a
  structural drift bites.
