# AIS-005 — Provenance schema + full UPDATE protocol

**Status:** done (2026-07-22)

## What shipped
- UPDATE (UC2) protocol authored in README (AIS-003) + provenance schema §4.2: find
  `.provenance.json` → compare per-path SHA via GitHub API (no clone) → show commit log
  before overwrite → `local_modified` 3-way STOP → re-stamp provenance.

## UC2 acceptance (spec §8 Phase 2) — passed live
Vehicle: a genuine `pr` neutrality fix (dropped residual `/pr` slash-command assumption),
commit `76694c7`, pushed. Scratch install of `pr` had provenance at the pre-fix sha `a5a0ece`.
- ✅ detected behind: API latest `76694c7` != installed `a5a0ece`.
- ✅ commit-log preview BEFORE overwrite: showed the 1 new commit correctly.
- ✅ applied: re-fetched SKILL.md, re-stamped provenance to `76694c7`.
- ✅ `local_modified: true` guard: STOP-and-ask (keep mine / take upstream / show diff).
