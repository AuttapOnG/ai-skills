# AIS-011 — xlsx-safe-export disposition (deferred from AIS-002)

**Status:** done (2026-07-22) — decision: **SKIP (do not publish)**

Context: xlsx-safe-export's core value is a Python validator, which the v1 markdown-only policy
(spec §6.2/§9) forbids shipping to the public registry.

Decision (user, 2026-07-22): **skip the feature.** xlsx-safe-export will NOT be migrated to the
public registry; it stays local-only (like update-tags). The markdown-only v1 policy stands
unchanged. Revisit only if a future "maintainer-tooling carve-out" to §6.2 is ever adopted.
