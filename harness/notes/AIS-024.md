# AIS-024 — init-harness v2 (verification tiers, orchestration, template fixes)

**Status:** done (2026-08-19)

## What shipped
- `skills/init-harness/SKILL.md`: appended Feature Workflow now routes work through three
  **Verification Tiers** keyed to behavior change (full TDD / existing tests / spike), with
  feature-scoped tests during red-green and one full-suite run before `done`. New **Q10
  orchestration style** (cost-optimized / balanced / speed-first) generates an
  **Agent Orchestration** appended section; subagent findings must be recorded in harness
  notes before a feature is set `done`. Duplicate "Context & Memory Rules" removed from the
  appended block (now lives in the CLAUDE.md/AGENTS.md templates). Spec templates moved to
  YAML frontmatter (`status:`) so init.sh's approved-spec grep actually matches; example spec
  set to `implemented` so fresh projects don't report a fake active spec. `.gitkeep`
  placeholders for `.claude/commands/` and `docs/plans/`. Description trimmed to triggering
  conditions only (SDO).
- `claude-md.md` / `agents-md.md`: session start now reads harness memory (progress.md +
  feature note); LOW autonomy gets explicit pre-approvals (init.sh, read-only git, harness
  memory updates), resolving its self-contradiction with "ask before any shell command";
  `WORKING_STATE.md` replaced by feature notes + progress.md — one memory system.
- `settings-json.md`: PERMISSIVE `allow: ["*"]` (invalid — unanchored globs are skipped with
  a warning) replaced by an explicit tool list; STRICT `Bash(git status*)`; deletion/network
  hooks anchored to command position (`(^|[;&|]) *`) and now cover `unlink`.
- `init-sh.md`: dropped the arbitrary `head -5` cap on spec detection.
- Verified: grep matches only truly-approved specs (template/example don't false-match);
  hook regexes block rm/rmdir/unlink/git clean/curl/wget/fetch including compound commands
  and pass innocent ones; PERMISSIVE JSON valid; 2× Haiku smoke tests pick the correct
  verification tier (typo → existing tests only; new validation rule → full TDD).
- `registry.json` regenerated.
