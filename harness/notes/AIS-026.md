# AIS-026 — init-harness token-weight refactor (templates → 3 reference files)

**Status:** done (2026-08-19)

## What shipped
- `skills/init-harness/SKILL.md` slimmed to workflow only (Steps 1–5, Q&A, variant
  mapping, seeding + merge rules): **1,571 words**, down ~48% from ~3,000 — this is what
  loads on every invocation. Total content unchanged, now split across on-demand files:
  - `scaffold-templates.md` (771 words) — SPEC-TEMPLATE, example spec, ADR template,
    auto-generated init ADR, eval template + README, otel stub + traces README.
  - `work-control.md` (920 words) — feature_list.json (+ entry schema, groups map),
    progress.md, note files, harness/README.md (update discipline incl. archiving),
    and the CLAUDE.md/AGENTS.md appended sections (Work Control, Feature Workflow,
    Verification Tiers, Agent Orchestration).
  - `references.md` (227 words) — the curated source list, previously ~30 lines inline.
- SKILL.md's Step 3 now names which template file holds each of the 18 generated
  artifacts; Step 4 keeps seeding + merge rules and points to work-control.md.
- Verified: signature grep confirms every moved template lives in exactly one new file
  with nothing left behind in SKILL.md; Haiku retrieval smoke test resolves the right
  template file for progress.md, eval-template, low-autonomy CLAUDE.md, and the
  Verification Tiers appended section.
- Follows the "Skill with Heavy Reference" pattern (superpowers writing-skills):
  frequently-loaded body stays lean, heavy reference loads only when generating.
