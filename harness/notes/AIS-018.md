# AIS-018 — README sync (License section, UPDATE new-skills step, meta-skills note)

**Status:** done (2026-07-22)

## What shipped
Three edits to [`README.md`](../../README.md):
1. **License section** near the end — points to [`LICENSE`](../../LICENSE) (MIT), states
   inbound=outbound, and notes the per-skill footer preserves attribution. (Depends on AIS-017.)
2. **UPDATE protocol synced to skill-updater/AIS-013** — inserted a new step 3, "Surface skills
   new in the registry" (fetch `registry.json`, list names with no local folder, offer to
   install opt-in). Renumbered old 3→4 … 6→7 and added two verify bullets (new-skills reported +
   installed only on confirmation; newly-installed provenance recorded). The README UPDATE flow
   had drifted behind the skill after AIS-013 added discovery; now aligned.
3. **Meta-skills note** — a callout after the registry.json index line saying the
   INSTALL/UPDATE/publish flows are also installable as `skill-updater` / `skill-publisher`,
   while the written protocol stays the source of truth.

## Notes
- README deliberately does **not** enumerate individual skills — `registry.json` is the index,
  so a new skill (skill-updater) needed no README listing change; only the protocol/section work
  above was warranted.
- No `skills/` files touched → CI's mechanical checks unaffected; these are root-doc edits.
