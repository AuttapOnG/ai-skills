# AIS-001 — Repo bootstrap

**Status:** in_progress (2026-07-22)

## Done
- Local git repo initialized (branch `main`), spec + harness committed.
- Harness scaffold via /init-harness: CLAUDE.md, AGENTS.md, init.sh
  (secret + personal-path + registry-sync scans), .claude/settings.json
  (push/deletion hooks), ADR-0001, evals/traces stubs.

## Remaining
- Confirm repo name (proposal: `ai-skills`).
- Create GitHub repo (public) and push.

## Notes
- Spec HTML review copies live in ~/Downloads (ai-skills-spec.html, ai-skills-usecases.html);
  markdown spec in harness/specs/ is canonical.
- init.sh personal-path scan greps for /Users/, C:\\, and company-internal
  strings — extend the pattern list as new leak classes are found.
