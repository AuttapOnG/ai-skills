# AIS-003 — README protocol (INSTALL/UPDATE)

**Status:** done (2026-07-22)

Acceptance criteria: spec §8 + harness/feature_list.json (AIS-003).

## What shipped
- `README.md` — addressed to AI agents; sections per spec §5: What is this / what a good skill
  looks like · INSTALL (UC1 checklist + provenance schema §4.2 + verbatim publish-line §4.4 +
  Verify) · UPDATE (UC2 checklist + commit-log-before-overwrite + `local_modified` 3-way + Verify)
  · EDIT & CONTRIBUTE → CONTRIBUTING.md.
- `CONTRIBUTING.md` — minimal stub (Decision #6): markdown-only, tool-neutral, frontmatter+footer,
  fork+PR / owner-push, Co-Authored-By required. Full checklist + branch protection deferred to AIS-006.

## Decisions
- Authored by Claude directly (NOT delegated to Codex): protocol/spec authoring stays with the
  coordinating agent per the cli-subagents skill ("never delegate spec + plan authoring").
- Publish-line and provenance schema copied verbatim from spec with `<you>` → `AuttapOnG`.

## Verification
- No `<you>` placeholders; no personal paths / company strings.
- Publish-line matches spec §4.4 exactly (owner substituted).
- init.sh clean.
